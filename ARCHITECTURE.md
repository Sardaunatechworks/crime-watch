# Crime Watch - Real-time Architecture

## System Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    CRIME WATCH APP                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              React Components                         │  │
│  │  ┌─────────────┬──────────────┬──────────────┐       │  │
│  │  │  Dashboard  │ IncidentForm │   Layout    │       │  │
│  │  └─────────────┴──────────────┴──────────────┘       │  │
│  └──────────────────────────────────────────────────────┘  │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          State Management (React Hooks)              │  │
│  │  • incidents (state)                                 │  │
│  │  • user (auth state)                                 │  │
│  │  • filter, search, loading states                    │  │
│  └──────────────────────────────────────────────────────┘  │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          Service Layer (db.ts)                       │  │
│  │  • dbService.getIncidents()                          │  │
│  │  • dbService.getUserIncidents()                      │  │
│  │  • dbService.addIncident()                           │  │
│  │  • dbService.updateIncidentStatus()                  │  │
│  │  • dbService.deleteIncident()                        │  │
│  │  • dbService.subscribeToIncidents() 🔄 REALTIME      │  │
│  │  • authService.login/logout                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│         Supabase JavaScript Client (supabase.ts)           │
│  • Handles database queries                                │
│  • Real-time subscriptions                                 │
│  • Error handling & logging                                │
└─────────────────────────────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              SUPABASE (Backend as a Service)               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │        PostgreSQL Database                           │  │
│  │  ┌──────────────────────────────────────────────┐    │  │
│  │  │ USERS TABLE                                  │    │  │
│  │  │ • id (UUID)                                  │    │  │
│  │  │ • email                                      │    │  │
│  │  │ • role (REPORTER | ADMIN)                    │    │  │
│  │  │ • created_at, last_login                     │    │  │
│  │  └──────────────────────────────────────────────┘    │  │
│  │                                                       │  │
│  │  ┌──────────────────────────────────────────────┐    │  │
│  │  │ INCIDENTS TABLE                              │    │  │
│  │  │ • id (UUID)                                  │    │  │
│  │  │ • reporter_id (FK)                           │    │  │
│  │  │ • title, category, location, description     │    │  │
│  │  │ • status (PENDING | UNDER_INVESTIGATION |    │    │  │
│  │  │           RESOLVED)                          │    │  │
│  │  │ • status_history (JSONB)                     │    │  │
│  │  │ • created_at, last_updated_at                │    │  │
│  │  └──────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Real-time Subscriptions (Postgres Triggers)         │  │
│  │  • Detects INSERT, UPDATE, DELETE on tables          │  │
│  │  • Broadcasts changes to subscribed clients          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Row Level Security (RLS) - Optional                 │  │
│  │  • Users can only see their own incidents            │  │
│  │  • Admins can see and modify all incidents           │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Real-time Flow Diagram

```
Browser 1 (Admin Dashboard)          Browser 2 (User Report)
        │                                    │
        │                                    │
        │                          ┌─────────▼──────────┐
        │                          │ Creates new        │
        │                          │ incident via form  │
        │                          └────────┬───────────┘
        │                                   │
        │                          ┌────────▼──────────┐
        │                          │ Sends to Supabase│
        │                          │ (INSERT query)   │
        │                          └────────┬──────────┘
        │                                   │
        │                    ┌──────────────▼─────────────┐
        │                    │   Supabase PostgreSQL      │
        │                    │   Incident created ✓       │
        │                    │   Triggers: INSERT event   │
        │                    └──────────────┬─────────────┘
        │                                   │
        │        ┌──────────────────────────▼──────────────────────┐
        │        │   Supabase Realtime (WebSocket)                 │
        │        │   Broadcasts: "incidents:INSERT" to all subs    │
        │        └──────────────────────────┬─────────────────────┘
        │                                   │
        ├───────────────────────────────────┘
        │
        ▼
    ┌──────────────────────┐
    │ Client receives      │
    │ real-time event      │
    │ via subscription     │
    │                      │
    │ Callback triggers:   │
    │ dbService.getIncidents()
    │ ↓ fetches fresh data │
    │ ↓ sets state         │
    │ ↓ dashboard updates  │
    └──────────────────────┘
         🎉 Instant display!
```

## Incident Lifecycle

```
1. PENDING ─────────────────────┐
   (Newly reported)             │
   ↓                            │
   User sees "Unassigned"       │
                                │
2. UNDER_INVESTIGATION ────┐   │
   (Admin processing)      │   │
   ↓                       │   │
   Real-time update        │   │
   Dashboard refreshes     │   │
   Status changes show up  │   │
                           │   │
3. RESOLVED ───────────────┘   │
   (Case closed)                │
   ↓                            │
   Final status update          │
   status_history records all   │
   transitions                  │
                                │
4. DELETED (Optional) ──────────┘
   (Admin purges record)
   ↓
   Removed from all views
```

## Data Flow Example

```
User Action: Create New Incident

    Event: Form Submit
         │
         ▼
    handleCreate() [Dashboard.tsx]
         │
         ├─ incidentData = {
         │    title: "Stolen Bike",
         │    category: "Theft",
         │    location: "Downtown Square",
         │    description: "Red mountain bike...",
         │    reporter_id: user.id,
         │    reporter_email: user.email
         │  }
         │
         ▼
    dbService.addIncident(data) [db.ts]
         │
         ├─ Add timestamp
         ├─ Set status = PENDING
         ├─ Create initial status_history
         │
         ▼
    supabase.from('incidents').insert() [supabase.ts]
         │
         ▼
    ✓ Data inserted into PostgreSQL
         │
         ▼
    🔔 Real-time event: INSERT
         │
         ▼
    Browser 1 (Admin) receives update
    Browser 2 (Reporter) receives update
         │
         ▼
    setIncidents() called in Dashboard
         │
         ▼
    🎉 New incident appears instantly
         (no refresh needed!)
```

## Performance Characteristics

| Operation       | Time       | Notes                      |
| --------------- | ---------- | -------------------------- |
| Create Incident | ~100-300ms | Network + DB insert        |
| Update Status   | ~50-200ms  | Network + DB update        |
| Delete Incident | ~50-200ms  | Network + DB delete        |
| Initial Load    | ~200-500ms | Depends on # of incidents  |
| Real-time Push  | ~10-50ms   | WebSocket broadcast        |
| Subscribe Setup | <100ms     | Subscription establishment |

## Security Features

```
┌─────────────────────────────────────┐
│     Row Level Security (RLS)        │
├─────────────────────────────────────┤
│ REPORTER USERS can:                 │
│ ✓ Read own incidents                │
│ ✓ Create new incidents              │
│ ✗ Cannot modify other incidents     │
│                                     │
│ ADMIN USERS can:                    │
│ ✓ Read all incidents                │
│ ✓ Update any incident status        │
│ ✓ Delete incidents                  │
│ ✓ View user information             │
└─────────────────────────────────────┘
```

## Deployment Ready ✓

- ✅ Database schema defined
- ✅ Real-time subscriptions enabled
- ✅ Error handling implemented
- ✅ TypeScript types complete
- ✅ Environment variables configured
- ✅ No hardcoded credentials
- ✅ Production-ready code structure
